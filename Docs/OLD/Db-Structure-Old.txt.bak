"allocation_rules_config"	8	7	"allocation_rules_config_pkey, allocation_rules_config_rule_name_key"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  rule_name (character varying(100) NOT NULL)
  rule_description (text)
  is_active (boolean DEFAULT true)
  weight (numeric DEFAULT 1.00)
  config_value (jsonb)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  updated_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"audit_logs"	10	0	"audit_logs_pkey, idx_audit_logs_created_at, idx_audit_logs_user_id"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  user_id (uuid REFERENCES users(id))
  action (character varying(100) NOT NULL)
  entity_type (character varying(50) NOT NULL)
  entity_id (uuid)
  old_values (jsonb)
  new_values (jsonb)
  ip_address (inet)
  user_agent (text)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"batch_schedules"	7	0	"batch_schedules_pkey, idx_batch_schedules_batch_id"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  batch_id (uuid REFERENCES batches(id))
  day_of_week (integer NOT NULL)
  start_time (time without time zone NOT NULL)
  end_time (time without time zone NOT NULL)
  duration_minutes (integer NOT NULL)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"batches"	14	0	"batches_pkey, idx_batches_batch_type, idx_batches_status, idx_batches_teacher_id"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  name (character varying(100) NOT NULL)
  batch_type (USER-DEFINED NOT NULL)
  is_inclusive (boolean DEFAULT false)
  max_capacity (integer NOT NULL DEFAULT 1)
  current_capacity (integer DEFAULT 0)
  age_group_min (integer NOT NULL)
  age_group_max (integer NOT NULL)
  teacher_id (uuid REFERENCES teachers(id))
  start_date (date NOT NULL)
  end_date (date)
  status (USER-DEFINED DEFAULT 'active'::batch_status)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  updated_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"student_batch_assignments"	10	0	"idx_student_batch_assignments_batch_id, idx_student_batch_assignments_student_id, student_batch_assignments_pkey, student_batch_assignments_student_id_batch_id_key"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  student_id (uuid REFERENCES students(id))
  batch_id (uuid REFERENCES batches(id))
  assignment_type (USER-DEFINED NOT NULL)
  assignment_date (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  admin_notes (text)
  match_score (numeric)
  status (USER-DEFINED DEFAULT 'active'::assignment_status)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  updated_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"student_time_preferences"	7	0	"idx_student_time_preferences_student_id, student_time_preferences_pkey"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  student_id (uuid REFERENCES students(id))
  day_of_week (integer NOT NULL)
  start_time (time without time zone NOT NULL)
  end_time (time without time zone NOT NULL)
  is_preferred (boolean DEFAULT true)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"students"	12	0	"idx_students_age_group, idx_students_student_type, idx_students_user_id, students_pkey"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  user_id (uuid REFERENCES users(id))
  date_of_birth (date NOT NULL)
  age_group (USER-DEFINED NOT NULL)
  student_type (USER-DEFINED DEFAULT 'normal'::student_type)
  class_type_preference (USER-DEFINED NOT NULL)
  learning_level (USER-DEFINED DEFAULT 'beginner'::learning_level)
  support_requirements (text)
  emergency_contact_name (character varying(100))
  emergency_contact_phone (character varying(20))
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  updated_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"teacher_availability"	7	0	"teacher_availability_pkey"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  teacher_id (uuid REFERENCES teachers(id))
  day_of_week (integer NOT NULL)
  start_time (time without time zone NOT NULL)
  end_time (time without time zone NOT NULL)
  is_available (boolean DEFAULT true)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"teachers"	9	0	"idx_teachers_user_id, teachers_pkey"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  user_id (uuid REFERENCES users(id))
  specialization (ARRAY)
  is_trained_special_needs (boolean DEFAULT false)
  max_students_per_batch (integer DEFAULT 10)
  hourly_rate (numeric)
  bio (text)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  updated_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)"
"users"	12	4	"idx_users_email, idx_users_role, users_email_key, users_pkey"	"id (uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY)
  email (character varying(255) NOT NULL)
  password_hash (character varying(255) NOT NULL)
  role (USER-DEFINED NOT NULL)
  first_name (character varying(100) NOT NULL)
  last_name (character varying(100) NOT NULL)
  phone (character varying(20))
  country (character varying(100) NOT NULL)
  timezone (character varying(50) NOT NULL)
  created_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  updated_at (timestamp without time zone DEFAULT CURRENT_TIMESTAMP)
  is_active (boolean DEFAULT true)"