"analytics"	7	46	"analytics_pkey, idx_analytics_created_at, idx_analytics_event_type, idx_analytics_user_id"	"id (integer NOT NULL DEFAULT nextval('analytics_id_seq'::regclass) PRIMARY KEY)
  user_id (integer REFERENCES users(id))
  event_type (character varying(100) NOT NULL)
  event_data (jsonb)
  ip_address (character varying(45))
  user_agent (text)
  created_at (timestamp without time zone DEFAULT now())"
"bookmarks"	6	0	"bookmarks_pkey, bookmarks_user_id_brand_id_retailer_id_key, idx_bookmarks_brand_id, idx_bookmarks_retailer_id, idx_bookmarks_user_id"	"id (integer NOT NULL DEFAULT nextval('bookmarks_id_seq'::regclass) PRIMARY KEY)
  user_id (integer REFERENCES users(id))
  brand_id (integer REFERENCES brands(id))
  retailer_id (integer REFERENCES retailers(id))
  notes (text)
  created_at (timestamp without time zone DEFAULT now())"
"brands"	17	6	"brands_pkey, idx_brands_category, idx_brands_region, idx_brands_user_id"	"id (integer NOT NULL DEFAULT nextval('brands_id_seq'::regclass) PRIMARY KEY)
  user_id (integer REFERENCES users(id))
  name (character varying(255) NOT NULL)
  category (character varying(100))
  description (text)
  website (character varying(255))
  region (character varying(100))
  format (character varying(100))
  logo_url (character varying(500))
  contact_email (character varying(255))
  contact_phone (character varying(20))
  founded_year (integer)
  employee_count (character varying(50))
  annual_revenue (character varying(100))
  is_verified (boolean DEFAULT false)
  created_at (timestamp without time zone DEFAULT now())
  updated_at (timestamp without time zone DEFAULT now())"
"file_uploads"	12	0	"file_uploads_pkey, idx_uploads_status, idx_uploads_user_id"	"id (integer NOT NULL DEFAULT nextval('file_uploads_id_seq'::regclass) PRIMARY KEY)
  user_id (integer REFERENCES users(id))
  filename (character varying(255) NOT NULL)
  original_name (character varying(255))
  file_path (character varying(500) NOT NULL)
  file_size (integer)
  mime_type (character varying(100))
  upload_type (character varying(50))
  status (character varying(20) DEFAULT 'pending'::character varying)
  error_message (text)
  created_at (timestamp without time zone DEFAULT now())
  updated_at (timestamp without time zone DEFAULT now())"
"fit_scores"	11	17	"fit_scores_brand_id_retailer_id_key, fit_scores_pkey, idx_fit_scores_brand_id, idx_fit_scores_calculated, idx_fit_scores_retailer_id, idx_fit_scores_score"	"id (integer NOT NULL DEFAULT nextval('fit_scores_id_seq'::regclass) PRIMARY KEY)
  brand_id (integer REFERENCES brands(id))
  retailer_id (integer REFERENCES retailers(id))
  score (integer)
  factors (jsonb)
  category_match (boolean)
  region_match (boolean)
  format_match (boolean)
  product_alignment_score (integer)
  calculated_at (timestamp without time zone DEFAULT now())
  created_at (timestamp without time zone DEFAULT now())"
"products"	19	6	"idx_products_annual_turnover, idx_products_brand_id, idx_products_category, idx_products_mrp, idx_products_pack_size, idx_products_sku, idx_products_trade_margin, products_pkey"	"id (integer NOT NULL DEFAULT nextval('products_id_seq'::regclass) PRIMARY KEY)
  brand_id (integer REFERENCES brands(id))
  name (character varying(255) NOT NULL)
  sku (character varying(100))
  category (character varying(100))
  subcategory (character varying(100))
  price (numeric)
  currency (character varying(3) DEFAULT 'USD'::character varying)
  description (text)
  specifications (jsonb)
  image_url (character varying(500))
  is_active (boolean DEFAULT true)
  created_at (timestamp without time zone DEFAULT now())
  updated_at (timestamp without time zone DEFAULT now())
  pack_size (character varying(100))
  mrp (numeric)
  avg_trade_margin (numeric)
  annual_turnover (numeric)
  trade_margin (numeric)"
"retailer_sku_data"	9	0	"idx_retailer_sku_data_product_id, idx_retailer_sku_data_retailer_id, retailer_sku_data_pkey, retailer_sku_data_retailer_id_product_id_key"	"id (integer NOT NULL DEFAULT nextval('retailer_sku_data_id_seq'::regclass) PRIMARY KEY)
  retailer_id (integer REFERENCES retailers(id))
  product_id (integer REFERENCES products(id))
  sku_name (character varying(255))
  sku_mrp (numeric)
  sku_contribution_percent (numeric)
  price_point_category (character varying(100))
  created_at (timestamp without time zone DEFAULT now())
  updated_at (timestamp without time zone DEFAULT now())"
"retailers"	31	5	"idx_retailers_buying_model, idx_retailers_category, idx_retailers_region, idx_retailers_retailer_type, idx_retailers_supply_type, idx_retailers_user_id, retailers_pkey"	"id (integer NOT NULL DEFAULT nextval('retailers_id_seq'::regclass) PRIMARY KEY)
  user_id (integer REFERENCES users(id))
  name (character varying(255) NOT NULL)
  category (character varying(100))
  region (character varying(100))
  format (character varying(100))
  description (text)
  website (character varying(255))
  logo_url (character varying(500))
  contact_email (character varying(255))
  contact_phone (character varying(20))
  store_count (integer)
  annual_revenue (character varying(100))
  target_audience (character varying(255))
  is_verified (boolean DEFAULT false)
  created_at (timestamp without time zone DEFAULT now())
  updated_at (timestamp without time zone DEFAULT now())
  retailer_type (character varying(50))
  categories_present (text)
  subcategories_present (text)
  buying_model (character varying(20))
  avg_credit_days (integer)
  asp_category (numeric)
  competitor_benchmark (text)
  category_size_value (numeric)
  category_size_units (integer)
  supply_type (character varying(50))
  price_point_sales_distribution (text)
  sku_names_under_subcategory (text)
  sku_mrp_values (text)
  sku_contribution_percent (text)"
"user_sessions"	9	0	"idx_sessions_expires, idx_sessions_token, idx_sessions_user_id, user_sessions_pkey"	"id (integer NOT NULL DEFAULT nextval('user_sessions_id_seq'::regclass) PRIMARY KEY)
  user_id (integer REFERENCES users(id))
  token (character varying(500) NOT NULL)
  refresh_token (character varying(500))
  expires_at (timestamp without time zone NOT NULL)
  ip_address (character varying(45))
  user_agent (text)
  is_active (boolean DEFAULT true)
  created_at (timestamp without time zone DEFAULT now())"
"users"	14	9	"idx_users_active, idx_users_email, idx_users_reset_token, idx_users_role, users_email_key, users_pkey"	"id (integer NOT NULL DEFAULT nextval('users_id_seq'::regclass) PRIMARY KEY)
  email (character varying(255) NOT NULL)
  password (character varying(255) NOT NULL)
  role (character varying(20) NOT NULL)
  company_name (character varying(255) NOT NULL)
  first_name (character varying(100))
  last_name (character varying(100))
  phone (character varying(20))
  is_active (boolean DEFAULT true)
  email_verified (boolean DEFAULT false)
  created_at (timestamp without time zone DEFAULT now())
  updated_at (timestamp without time zone DEFAULT now())
  reset_token (character varying(255))
  reset_token_expiry (timestamp without time zone)"